/**
 * @swagger
 * tags:
 *   name: Catalog
 *   description: Bundled shop catalog (public)
 */

/**
 * @swagger
 * /api/catalog:
 *   get:
 *     summary: Full shop catalog bundle
 *     description: Returns brands, categories, and published products in one payload for the storefront.
 *     tags: [Catalog]
 *     responses:
 *       200:
 *         description: Shop catalog
 */

module.exports = {};
